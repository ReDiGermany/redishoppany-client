import { StyleSheet } from 'react-native'

const NavigationTitleStyle = StyleSheet.create({
  subtitle: {
    color: '#fff',
    position: 'absolute',
    bottom: 10,
    left: 15,
    fontSize: 10,
    opacity: 0.5,
  },
}).subtitle

export default NavigationTitleStyle
