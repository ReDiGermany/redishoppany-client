import React, { Component } from 'react'
import { View } from 'react-native'
import { lineStyle } from '../../styles/SidebarStyle'

export default class SidebarLine extends Component {
  render() {
    return <View style={lineStyle}></View>
  }
}
