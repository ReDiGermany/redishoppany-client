android {
  defaultConfig {
    missingDimensionStrategy 'react-native-camera', 'general'
  }
}
buildscript {
  dependencies  {
    compile ("com.facebook.react:react-native:0.64.0") { force = true }

  }
}